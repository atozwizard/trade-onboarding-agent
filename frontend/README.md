# Frontend (React + Vite)

## 실행

```bash
npm install
npm run dev
```

기본 개발 URL: `http://localhost:3000`

## 환경변수

`.env` 또는 `.env.local`에 아래 값을 설정할 수 있습니다.

```env
VITE_API_BASE_URL=http://localhost:8000/api
```

설정하지 않으면 기본값 `http://localhost:8000/api`를 사용합니다.
